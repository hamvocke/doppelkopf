version: '3'
services:
  frontend:
    restart: always
    build: "./frontend"
    ports:
      - "8088:80"
    depends_on:
      - backend

  backend:
    restart: always
    build: .
    expose:
      - "5000"
    ports:
      - "5000:5000"
